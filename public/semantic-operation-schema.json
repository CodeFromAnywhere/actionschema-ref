{
  "$schema": "https://spec.actionschema.com/actionschema.json",
  "description": "Meta Schema that defines a schema that defines a semantic operation.",
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "additionalProperties"],
  "properties": {
    "type": { "type": "string", "enum": ["object"], "default": "object" },
    "description": {
      "type": "string",
      "description": "Combined description of all semantics"
    },
    "additionalProperties": {
      "type": "boolean",
      "default": false,
      "enum": [false]
    },
    "required": { "type": "array", "items": { "type": "string" } },
    "properties": {
      "$ref": "https://spec.actionschema.com/semantic-operation.json"
    }
  }
}
