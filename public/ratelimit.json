{
  "$comment": "WIP",
  "description": "Ratelimiting extension by ActionSchema. Can be applied globally, per plan, per tag, or per operation",
  "type": "object",
  "properties": {
    "limit": { "type": "number" },
    "interval": { "type": "string", "enum": ["second", "minute"] }
  }
}
